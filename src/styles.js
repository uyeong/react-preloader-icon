const blind = {
    fontSize: 0,
    height: 0,
    left: 0,
    lineHeight: 0,
    overflow: 'hidden',
    position: 'absolute',
    top: 0,
    visibility: 'hidden',
    width: 0
};

const inner = {
    position: 'relative',
    overflow: 'hidden',
    transform: 'translate3d(0, 0, 0)'
};

export {blind, inner};
